# Ozora Festival Companion 2.1

Upload all files to the root of the GitHub repository. In GitHub Pages select `main` and `/(root)`.

Public URL: `https://mudita1973.github.io/Ozora2026_2.0/`

The package contains `index.html`, so the full filename is no longer required. Version 2.1 uses a new service-worker cache. After deployment, reload once. On iOS, remove the old home-screen installation and add it again if the old icon or cached version persists.
